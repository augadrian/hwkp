<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="OrderMapper">

    <insert id="addOrder">
		insert into b_order( order_id, transaction_id, round_id, product_id,zip_code,province_name,
		city_name,county_name,detail_info,tel_number,user_name,wx_openid,nonce_str,is_paid,paid_time,pay_result,express_num,
		is_send,send_time,is_active,created_user,created_time,modified_user,modified_time)
		values(#{orderId}, #{transactionId}, #{roundId}, #{productId},#{zipCode }
		,#{provinceName},#{cityName},#{countyName},#{detailInfo},#{telNumber},#{userName},#{wxOpenid},
		#{nonceStr},#{isPaid},#{paidTime},#{payResult},#{expressNum},#{isSend},#{sendTime},#{isActive},
		#{createdUser},#{createdTime},#{modifiedUser},#{modifiedTime})
	</insert>

    <update id="updateOrder">
		update b_order set order_id = #{orderId}, transaction_id = #{transactionId}, round_id = #{roundId},product_id=#{productId}
		,zip_code=#{zipCode},province_name=#{provinceName},city_name=#{cityName},
		detail_info=#{detailInfo},tel_number=#{telNumber},user_name=#{userName},wx_openid=#{wxOpenid},nonce_str=#{nonceStr},
		is_paid=#{isPaid},paid_time=#{paidTime},pay_result=#{payResult},
		express_num=#{expressNum},is_send=#{isSend},send_time=#{sendTime},is_active=#{isActive},
		created_user=#{createdUser},created_time=#{createdTime}
		,modified_user=#{modifiedUser},modified_time=#{modifiedTime} where id = #{id}
	</update>

    <delete id="deleteOrder">
        delete from b_order where id in
        <foreach collection="list" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
    </delete>


</mapper>